import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // Identificador único en App Store y Google Play: no se puede cambiar después de publicar
  appId: 'com.elkinjimenez.foodplanner',
  appName: 'Food Planner',
  webDir: 'www'
};

export default config;
